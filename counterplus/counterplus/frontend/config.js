// Set this to your deployed backend's URL once you have one (from Render/Railway).
// Leave as empty string "" to use the same origin the frontend is served from
// (this is what you want for local testing, where one server does both).
//
// Example once deployed: const BACKEND_URL = "https://counterplus-api.onrender.com";
const BACKEND_URL = "";
